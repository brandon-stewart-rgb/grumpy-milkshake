

function Header() {
    
    return (
            <section class="hero is-primary">
                <div class="hero-body">
                    <p class="subtitle">{document.title}</p>
                </div>
            </section>
    )

}

export default Header;